import styled from 'styled-components';

export const Buttom = styled.button`
  position: relative;
  bottom: 50%;
  transform: translate(-200%,50%);
  font-size: 32px;
  border: none;
  cursor: pointer;
  text-shadow: none;
  background:none;
  color: #42f563;
`